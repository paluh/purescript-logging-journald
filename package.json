{
  "license": "BSD-3-Clause",
  "private": true,
  "scripts": {
    "clean": "rimraf output && rimraf .pulp-cache",
    "format": "purs-tidy format-in-place src test",
    "build": "eslint src && pulp build -- --censor-lib --strict",
    "test": "pulp test"
  },
  "devDependencies": {
    "eslint": "^4.4.1",
    "pulp": "^11.0.2",
    "purs-tidy": "^0.6.2",
    "rimraf": "^2.6.1"
  },
  "dependencies": {
    "prelude": "^0.8.2",
    "systemd": "^0.4.0",
    "systemd-journald": "^1.5.2"
  }
}
